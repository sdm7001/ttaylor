# coder -- 2026-04-21

Invocation: Phase 2 architecture documents for Ttaylor family law paralegal platform

## What I Did

Wrote 5 Phase 2 architecture documents based on SCHEMA_CANON.md (45+ tables across 10 sections):

1. **ERD.md** (882 lines) - Complete Mermaid erDiagram covering all entities with cardinality. Includes design decision explanations for UUID PKs, soft deletes, append-only audit, file_assets as central reference, contact/portal separation, stage transitions.

2. **STATE-MACHINES.md** (298 lines) - 4 state machines (Matter 14 states, Document 10 states, Filing Packet 11 states, Checklist Item 8 states). Each has Mermaid stateDiagram-v2, transition table with From/To/Trigger/Actor/System Actions/Gates. Documented attorney approval hard gates, Harris County e-filing constraints, WAIVED vs SKIPPED semantics.

3. **MODULE-BOUNDARIES.md** (680 lines) - 15 modules fully specified with owned tables, tRPC procedures (type: query/mutation), external inputs, emitted events/jobs, boundary rules. Includes Mermaid dependency graph. Added Orders/Compliance as cross-cutting concern under Matters module.

4. **RBAC-MATRIX.md** (377 lines) - 6 roles x 12 resource categories matrix. Implementation notes cover middleware chain (auth -> permission -> matterScope), matter sensitivity levels (STANDARD/RESTRICTED/ACP_PROTECTED), attorney approval gate enforcement, portal access isolation.

5. **API-CONVENTIONS.md** (986 lines) - 10 sections covering router organization, naming, Zod input patterns, output shapes, auth middleware, cursor-based pagination, filtering, audit emission, error codes (PRECONDITION_FAILED for gate failures), BullMQ job dispatch. Complete example: matters.updateStatus showing all conventions.

## Key Decisions

- Schema canon has `matter_parties` (not `matter_contacts` as originally in prompt) and `filing_packet_items` (not `filing_packet_documents`). I used the actual schema canon names.
- Schema has `orders` and `compliance_items` tables not mentioned in the prompt's entity groups. Included them under Matters module.
- Schema has `children`, `employment_records`, `matter_stage_transitions`, `settlement_proposals`, `evidence_items`, `communication_threads`, `communication_messages`, `filing_events`, `service_records`, `tasks` -- all included in ERD and module boundaries.
- Notifications module has no database tables (stateless BullMQ worker). Documented this explicitly.
- The schema uses `templates` (not `document_templates`) for the document template table.

## What to Remember

- The SCHEMA_CANON.md is the authoritative source. Always cross-reference entity names against it, not the task description.
- The schema has ~45 tables total, organized into 10 sections. The prompt mentioned some table names that don't match the canon (e.g., `contact_phone_numbers` doesn't exist -- phone is on the contacts table directly).
