# coder -- 2026-04-21

Invocation: Phase 2 architecture documents for Ttaylor family law paralegal platform

## What I Did

Wrote 5 Phase 2 architecture documents based on SCHEMA_CANON.md (45+ tables across 10 sections):

1. **ERD.md** (882 lines) - Complete Mermaid erDiagram covering all entities with cardinality. Includes design decision explanations for UUID PKs, soft deletes, append-only audit, file_assets as central reference, contact/portal separation, stage transitions.

2. **STATE-MACHINES.md** (298 lines) - 4 state machines (Matter 14 states, Document 10 states, Filing Packet 11 states, Checklist Item 8 states). Each has Mermaid stateDiagram-v2, transition table with From/To/Trigger/Actor/System Actions/Gates. Documented attorney approval hard gates, Harris County e-filing constraints, WAIVED vs SKIPPED semantics.

3. **MODULE-BOUNDARIES.md** (680 lines) - 15 modules fully specified with owned tables, tRPC procedures (type: query/mutation), external inputs, emitted events/jobs, boundary rules. Includes Mermaid dependency graph. Added Orders/Compliance as cross-cutting concern under Matters module.

4. **RBAC-MATRIX.md** (377 lines) - 6 roles x 12 resource categories matrix. Implementation notes cover middleware chain (auth -> permission -> matterScope), matter sensitivity levels (STANDARD/RESTRICTED/ACP_PROTECTED), attorney approval gate enforcement, portal access isolation.

5. **API-CONVENTIONS.md** (986 lines) - 10 sections covering router organization, naming, Zod input patterns, output shapes, auth middleware, cursor-based pagination, filtering, audit emission, error codes (PRECONDITION_FAILED for gate failures), BullMQ job dispatch. Complete example: matters.updateStatus showing all conventions.

## Key Decisions

- Schema canon has `matter_parties` (not `matter_contacts` as originally in prompt) and `filing_packet_items` (not `filing_packet_documents`). I used the actual schema canon names.
- Schema has `orders` and `compliance_items` tables not mentioned in the prompt's entity groups. Included them under Matters module.
- Schema has `children`, `employment_records`, `matter_stage_transitions`, `settlement_proposals`, `evidence_items`, `communication_threads`, `communication_messages`, `filing_events`, `service_records`, `tasks` -- all included in ERD and module boundaries.
- Notifications module has no database tables (stateless BullMQ worker). Documented this explicitly.
- The schema uses `templates` (not `document_templates`) for the document template table.

## What to Remember

- The SCHEMA_CANON.md is the authoritative source. Always cross-reference entity names against it, not the task description.
- The schema has ~45 tables total, organized into 10 sections. The prompt mentioned some table names that don't match the canon (e.g., `contact_phone_numbers` doesn't exist -- phone is on the contacts table directly).

---

## Phase 8: Release Packaging

### What I Did

Wrote 6 Phase 8 release packaging documents (1,793 total lines):

1. **docs/runbooks/DEPLOYMENT.md** (596 lines) - Complete DigitalOcean VPS deployment guide: prerequisites, non-root user setup, PostgreSQL 16 + Redis 7 install, env var reference, prisma migrate deploy, npm run build, pm2 ecosystem.config.js (staff port 3000, portal port 3001), full nginx reverse proxy configs for both subdomains with SSL/security headers, certbot commands, health checks, rollback procedures, deployment checklist.

2. **docs/runbooks/BACKUP-RECOVERY.md** (365 lines) - RPO 24h / RTO 4h targets. pg_dump daily backup script with cron, DO managed DB backups, WAL archiving for PITR. S3 versioning + cross-region replication. Restore procedures for pg_dump and PITR. Monthly backup verification script. Backup freshness monitoring.

3. **docs/runbooks/OPERATIONS.md** (419 lines) - Key metrics table (PG connections, Redis memory, disk, response time, SSL expiry). Health check script. Log locations table. Common issues: connection exhaustion, Redis OOM, stale Next.js cache, Clerk webhook failures, pm2 crash loop, disk full. Maintenance window procedure. User management via Clerk. Clerk org config for staff + portal.

4. **docs/runbooks/ONBOARDING.md** (278 lines) - Non-technical staff guide: system overview, 8 matter lifecycle phases, 5 roles explained, sign-in walkthrough, sidebar navigation, lead intake + conflict check, document generation + attorney review workflow, filing packet assembly + validation, calendar + auto-deadlines, client portal access + sharing, help contacts.

5. **READY_FOR_RELEASE.md** (83 lines) - Production readiness checklist: Architecture (9 items checked), Database (3 checked), Testing (4 checked), Security (6 checked), Operations (7 checked), Documentation (12 checked). Outstanding before go-live: 10 unchecked items (Clerk keys, DATABASE_URL, S3, SSL, DNS, SMTP, templates, admin user, eFileTexas, smoke test).

6. **STATUS.md** (52 lines) - Rewritten to reflect all 8 phases complete. Lists core application, data layer, business logic packages, legal controls. Next steps for pilot: provision VPS, configure Clerk, set up DB, run seeds, load templates, create admin, run E2E.

---

## Phase 3: Design System + App Shell Scaffold

### What I Did

Scaffolded the complete Phase 3 design system and app shell. Created 21 source files across 3 packages:

**packages/ui** (shared component library): package.json, tsconfig.json, tokens.ts, Button.tsx, Badge.tsx, Card.tsx, PageHeader.tsx, EmptyState.tsx, LoadingSpinner.tsx, StatusPill.tsx, DataTable.tsx, index.ts barrel

**packages/domain** (shared types): package.json, enums.ts (10 enums), types.ts (25+ interfaces), index.ts barrel

**apps/staff-web** (Next.js 14): package.json (Next.js 14, Clerk, tRPC, TanStack Query), tsconfig.json (path aliases), next.config.ts, root layout (ClerkProvider + Inter font), sign-in page (Clerk), dashboard layout (224px sidebar, 10 nav items), dashboard page (4 stat cards + empty activity), matters page (filter tabs + DataTable), middleware (Clerk), tRPC client, tRPC API route handler (placeholder router)

### Key Decisions
- Inline styles in UI library (no Tailwind) -- bare component library per spec
- Status color mappings cover all enum values from domain
- tRPC route uses placeholder empty router since services/api not built yet
- Only TODOs are for API data fetching

### What to Remember
- tokens.ts is the single source of truth for design tokens
- DataTable is fully generic: `DataTable<YourType>`
- Domain enums match DESIGN_SYSTEM_SPEC.md + SCHEMA_CANON.md status values
- Sidebar: exact 224px, #0f172a bg, 3px accent border on active item

---

## Phase 4: Platform Foundation Scaffold

### What I Did

Scaffolded the complete Phase 4 platform foundation -- 16 files of real working code:

**services/api (tRPC API service) -- 9 files**
- `package.json` -- dependencies: @trpc/server, @prisma/client, zod, superjson, @clerk/backend
- `src/prisma.ts` -- PrismaClient singleton with hot-reload leak prevention
- `src/context.ts` -- Clerk-based auth context creation, loads user + roles from DB
- `src/trpc.ts` -- tRPC init with superjson, publicProcedure, protectedProcedure, requirePermission, requireRole, createCallerFactory
- `src/routers/audit.ts` -- emitAuditEvent service function + read-only audit.list (ADMIN/ATTORNEY)
- `src/routers/matters.ts` -- list, getById, create, updateStatus (with state machine), assign
- `src/routers/documents.ts` -- list, getById, generate, submitForReview, approve, reject
- `src/routers/intake.ts` -- createLead, getLeads, convertToMatter, runConflictCheck
- `src/router.ts` -- appRouter composition
- `src/index.ts` -- barrel export

**packages/auth (RBAC helpers) -- 3 files**
- `src/permissions.ts` -- 30 permission constants covering all modules
- `src/roles.ts` -- RoleLevel enum + ROLE_PERMISSIONS mapping + resolvePermissions helper
- `src/index.ts` -- barrel export

**database/seeds -- 1 file**
- `seed.ts` -- Seeds roles, permissions, role-permission associations, 9 matter types, demo admin user

**ops -- 3 files**
- `docker-compose.yml` -- postgres:16, redis:7-alpine, adminer with healthchecks
- `.env.example` -- Updated with Clerk vars, preserving existing S3/SMTP/portal vars
- `Makefile` -- dev, db-reset, db-migrate, db-seed, typecheck, test, build targets

### Key Decisions
- Used Prisma PascalCase model names (Matter, User, etc.) matching schema.prisma
- Status transition state machine as VALID_TRANSITIONS map in matters router
- emitAuditEvent is a service function, not a tRPC route -- called from all mutations
- Conflict check searches MatterParty with adverseFlag=true on active matters
- Document generation: DRAFT -> GENERATED immediately (BullMQ dispatch as placeholder for Phase 5)
- Clerk integration via verifyToken from @clerk/backend
- RoleLevel enum duplicated in auth package to avoid @prisma/client dependency

### What to Remember
- Schema uses `cuid()` IDs -- Zod validators use `.cuid()` not `.uuid()`
- MatterStatus has 14 states, DocumentLifecycle has 10 states
- Audit entity uses `entityType`/`entityId` (adapted from API-CONVENTIONS.md `resourceType`/`resourceId` to match actual Prisma schema)
- The `.env.example` was updated to merge Clerk vars with pre-existing S3/SMTP/portal vars

---

## 6 Critical Fixes: tRPC Provider, State Machine, Dashboard/Matters/Contacts/Calendar Wiring

### What I Did

Fixed 6 blockers in the Ttaylor family law staff-web app:

1. **FIX 1 - tRPC Provider (CRITICAL)**: Created `trpc-provider.tsx` with QueryClient + trpc client setup (httpBatchLink, superjson transformer, 30s staleTime). Updated `trpc.ts` to import `AppRouter` from `@ttaylor/api` (was using placeholder `Record<string, never>`). Wrapped `{children}` in `<TrpcProvider>` in root `layout.tsx`.

2. **FIX 2 - State Machine Divergence (CRITICAL)**: Removed local `VALID_TRANSITIONS` map from `matters.ts` that used wrong status strings (LEAD_PENDING, CONFLICT_REVIEW, OPEN_ACTIVE, etc.) not matching `MatterStatus` enum. Replaced with import of `validateMatterTransition` and `ATTORNEY_REQUIRED_TRANSITIONS` from `@ttaylor/workflows`. Added `@ttaylor/workflows` as workspace dependency to `services/api/package.json`. Now uses `Object.values(MatterStatus)` for Zod enum instead of hardcoded array.

3. **FIX 3 - Dashboard wired**: Converted to `'use client'`, added `trpc.matters.list.useQuery({ status: 'ACTIVE', limit: 1 })` for active matters count, `trpc.calendar.getUpcoming.useQuery({ days: 7 })` for deadline count. Pending docs and filing queue left as 0 with TODO for aggregate endpoint.

4. **FIX 4 - Matters list wired**: Replaced empty `Matter[]` with `trpc.matters.list.useQuery()` passing status filter. Row click navigates to `/matters/${id}`. New Matter goes to `/matters/new`. Columns adapted to Prisma response shape (assignmentRole not role, matterType is object with .name).

5. **FIX 5 - Contacts list wired**: Replaced empty array with `trpc.contacts.list.useQuery({ search, limit: 50 })`. Row click navigates to `/contacts/${id}`. New Contact goes to `/contacts/new`.

6. **FIX 6 - Calendar wired**: Replaced empty arrays with `trpc.calendar.getUpcoming.useQuery({ days: 14 })`. Maps `data.deadlines` and `data.courtEvents` to existing UI cards. Added loading states.

### Key Decisions
- The Prisma response shape differs from domain `Matter` interface (e.g. `matterType` is `{ id, name, ... }` object not string, `assignmentRole` not `role`). Used `any` type for MatterRow with explicit render functions.
- Calendar `getUpcoming` returns `{ deadlines, courtEvents }`, not a flat array. Dashboard sums both for total count.
- The `trpc.ts` import changed from relative path `../../../../services/api/src/root` to workspace package `@ttaylor/api`.

### What to Remember
- MatterStatus enum (domain): LEAD, INTAKE, CONFLICT_CHECK, RETAINER_PENDING, OPEN, ACTIVE, DISCOVERY, NEGOTIATION, MEDIATION, TRIAL_PREP, ON_HOLD, PENDING_CLOSE, CLOSED, ARCHIVED
- The old matters.ts had wrong statuses: LEAD_PENDING, CONSULTATION_COMPLETED, RETAINED, OPEN_ACTIVE, AWAITING_FILING, etc. -- these were from Phase 4 scaffold before Phase 2 enums were finalized
- Calendar router has no `getUpcoming` that returns flat array -- it returns `{ deadlines, courtEvents }`

---

## Wiring Matter Detail, Filing Queue, Filing Detail, and Discovery to Real tRPC APIs

### What I Did

Rewrote 4 pages to replace stub/placeholder data with real tRPC API calls:

1. **Matter Detail** (`matters/[id]/page.tsx`) -- Replaced `useMatterData` stub with `trpc.matters.getById.useQuery`. Added queries for documents (`trpc.documents.list`), checklists (`trpc.checklists.getForMatter`), calendar (`trpc.calendar.listEvents`). Wired mutations: `documents.submitForReview`, `documents.approve`, `documents.reject`, `checklists.completeItem`. All mutations invalidate relevant queries on success via `trpc.useUtils()`.

2. **Filing Queue** (`filing/page.tsx`) -- Replaced static empty `packets` array with `trpc.matters.list.useQuery` showing matters as clickable rows navigating to matter detail. No aggregate `filing.listQueue` endpoint exists yet (TODO).

3. **Filing Detail** (`filing/[id]/page.tsx`) -- Replaced null `packet` and hardcoded role with `trpc.filing.getPacket.useQuery` and `trpc.filing.validatePacket.useQuery`. Wired 4 mutations: `submitForAttorneyReview`, `attorneyApprove`, `attorneyReject` (with window.prompt for reason), `submitToCourt`. All invalidate packet/validation queries on success.

4. **Discovery** (`discovery/page.tsx`) -- Replaced empty array with `trpc.matters.list.useQuery({ status: 'ACTIVE' })` showing active matters. Row click navigates to `/matters/${id}`.

### Key Patterns
- `trpc.useUtils()` is the pattern for cache invalidation in this codebase
- EmptyState component uses `heading`/`body` props, NOT `title`/`description`
- Checklist items have nested `checklistTemplateItem.title` for the display name
- Filing and Discovery pages need aggregate endpoints to avoid N+1 queries

### What to Remember
- Document lifecycle statuses: DRAFT, GENERATED, INTERNAL_REVIEW, ATTORNEY_REVIEW_REQUIRED, APPROVED, SIGNED, FILED, REJECTED, SUPERSEDED, ARCHIVED
- Filing packet statuses: ASSEMBLING, DOCUMENTS_COMPLETE, READY_FOR_ATTORNEY_REVIEW, ATTORNEY_APPROVED, ATTORNEY_REJECTED, SUBMITTED_TO_COURT, ACCEPTED, REJECTED_BY_COURT, DEFICIENCY_NOTICE, CORRECTING, ARCHIVED
- node_modules not installed -- cannot run tsc/lint to verify

---

## Phase 4 Legal Operations UI

### What I Did

Built 7 deliverables for Phase 4 legal operations UI:

1. **documents.listTemplates endpoint** -- Added `listTemplates` procedure to `services/api/src/routers/documents.ts`. Queries Template table with optional matterTypeId filter using `applicableMatterTypes: { has: matterTypeId }`.

2. **GenerateDocumentDialog** (`apps/staff-web/src/components/documents/GenerateDocumentDialog.tsx`) -- Modal dialog for document generation from templates. Features: template selector dropdown with `trpc.documents.listTemplates.useQuery`, empty-state message listing standard Texas family law templates when no templates configured, merge data form with fields from STANDARD_MERGE_FIELDS (matter, petitioner, respondent, attorney categories), pre-fill from matter context (causeNumber, court, judge, parties, assignments), generate button calling `trpc.documents.generate.useMutation`.

3. **Matter detail Documents tab** -- Added "Generate Document" button + GenerateDocumentDialog to DocumentsTab in `matters/[id]/page.tsx`. Imported `Plus` icon and `GenerateDocumentDialog`. Added `useState` for dialog open state. On success, invalidates documents list.

4. **Orders & Compliance page** (`apps/staff-web/src/app/(dashboard)/orders/page.tsx`) -- Queries `trpc.orders.listComplianceItems.useQuery({ upcomingDays: 30 })`. Shows table: Matter, Order Type, Compliance Item, Due Date (red if overdue + AlertTriangle icon), Responsible Party (parsed from completionNote), Status (custom ComplianceStatusPill with 5 statuses). Row click navigates to matter. Card at bottom links to Matters page for full order history.

5. **Audit Log page** (`apps/staff-web/src/app/(dashboard)/audit/page.tsx`) -- Queries `trpc.audit.list.useQuery`. Filters: event type dropdown (12 types), matter ID text input (client-side filtering via payloadJson.matterId), clear filters button. Table: Timestamp, Event Type (colored pills), Actor (truncated ID), Entity Type, Entity ID, Matter link. Cursor-based pagination with "Load More" button.

6. **Sidebar nav updated** (`layout.tsx`) -- Added: Orders (Scale icon) between Discovery and Financial, Audit Log (ShieldCheck icon) at the bottom after Settings. Intake (ClipboardList) was already present from a prior edit.

7. **Reports page wired** (`reports/page.tsx`) -- Replaced console.log buttons:
   - "Matter Pipeline": Uses `trpc.matters.list.useQuery({ limit: 100 })`, computes counts by status client-side, shows table with status/count/bar chart
   - "Upcoming Deadlines": Uses `trpc.calendar.getUpcoming.useQuery({ days: 30 })`, shows sorted list of deadlines + court events
   - Other 4 reports: Show amber "Requires additional API endpoints" message instead of console.log
   - Toggle behavior: clicking "Run Report" shows/hides results

### Key Patterns
- GenerateDocumentDialog builds nested merge data from flat key-value pairs for the generate mutation
- Compliance items parse "Responsible: ..." from completionNote field (schema design decision)
- Audit page does client-side matter ID filtering since the server filters entityType=Matter when matterId given
- Reports page uses toggle state pattern: activeReport string controls which report renders

### What to Remember
- Template model uses `applicableMatterTypes` as an array field (Prisma `has` filter)
- Orders router `listComplianceItems` includes order.matter relation for navigation
- Audit router returns `payloadJson` which may contain matterId for cross-reference
- Calendar `getUpcoming` returns `{ deadlines, courtEvents }` shape

## Phase 2 Core Operational Loop

### What I Did

Implemented 4 deliverables for Phase 2 core operational loop:

1. **Dashboard aggregate endpoint** (`services/api/src/routers/dashboard.ts`) -- New `dashboard.getSummary` tRPC query running 5 parallel Prisma queries: active matters count (6 statuses), pending documents count (NOT IN FILED/ARCHIVED), filing queue count (NOT IN ACCEPTED/COMPLETED/REJECTED via readinessStatus), upcoming deadlines (7 days, not completed), and 10 most recent audit events with actor name. Registered in `router.ts` alongside filing router (which existed but was never registered).

2. **Dashboard page rewrite** (`apps/staff-web/src/app/(dashboard)/page.tsx`) -- Replaced individual queries with single `trpc.dashboard.getSummary.useQuery()`. All 4 stat cards now show real numbers. Activity feed shows event type (human-readable label), actor name, and relative time. LoadingSpinner while loading, empty state when no activity.

3. **New Matter form** (`apps/staff-web/src/app/(dashboard)/matters/new/page.tsx`) -- 4-step form with Stepper UI: select matter type (9 hardcoded Texas family law types), client info (with conflict check via `trpc.intake.runConflictCheck`), matter details (cause number, court, judge, attorney), confirmation with summary. Creates lead via `trpc.intake.createLead` then converts via `trpc.intake.convertToMatter`. Full validation per step.

4. **New Contact form** (`apps/staff-web/src/app/(dashboard)/contacts/new/page.tsx`) -- Simple form: first/last name (required), email, phone, contact type dropdown (8 types matching contacts router), org name, notes. Calls `trpc.contacts.create.useMutation`. Success message with redirect to /contacts. Cancel navigates back.

5. **Filing tab on matter detail** (`apps/staff-web/src/app/(dashboard)/matters/[id]/page.tsx`) -- Replaced stub EmptyState with functional filing tab: queries `trpc.filing.listPackets`, shows packet list with status pills and "View" links. Inline form to create filing packets with title, filing type dropdown (7 types), court name (pre-filled from matter), cause number (pre-filled from matter). Form validation prevents empty required fields.

### Key Decisions

- Filing router was already created (filing.ts existed) but was never registered in router.ts. Added both `dashboardRouter` and `filingRouter` imports.
- Prisma schema has `readinessStatus` (FilingPacketStatus enum) on FilingPacket, not `status`. The dashboard query uses `readinessStatus` per Prisma schema, while the existing filing tRPC router references `status` (from its own migration). Dashboard query follows the Prisma schema.
- Deadline model has a `status` string field (not enum), with `completed` as the terminal value. Dashboard filters `status: { not: 'completed' }`.
- AuditEvent doesn't have matterId directly; the dashboard maps entityType/entityId but sets matterId to null in the feed.
- Matter types are hardcoded in the New Matter form until a `matterTypes.list` endpoint is created.

### What to Remember

- Filing router uses `status` field with values like ASSEMBLING, etc., but Prisma schema uses `readinessStatus` with FilingPacketStatus enum (NOT_STARTED, IN_PREPARATION, etc.). This divergence needs reconciliation.
- Deadline model uses plain string `status` field, not an enum. Values include "open" and "completed".
- The `contacts.create` mutation expects `contactType` as a required field (matching the contacts router Zod schema).
- The `filing.listPackets` returns `title`, `filingType`, `status`, `createdAt`, `preparedBy` -- matches the existing filing router shape.

---

## Phase 6: Search, Notes, Risk View (Invocation 2)

### What Was Done
- Created `Note` model in Prisma schema (database/schema/schema.prisma) with matter/author relations and privileged flag
- Added `notes` relation to Matter and User models in schema
- Created `searchRouter` (services/api/src/routers/search.ts) with global search across matters, contacts, documents using Prisma `contains` with `mode: insensitive`
- Created `notesRouter` (services/api/src/routers/notes.ts) with listForMatter and create procedures plus audit events
- Registered both routers in root router (services/api/src/router.ts)
- Created search page (apps/staff-web/src/app/(dashboard)/search/page.tsx) with debounced input, keyboard shortcut, 3-section results
- Created risk page (apps/staff-web/src/app/(dashboard)/risk/page.tsx) with overdue deadlines, matters on hold, compliance violations
- Updated sidebar layout with Search (after Dashboard) and Risk View (after Reports) nav items
- Replaced stub NotesTab in matter detail page with real implementation using trpc.notes queries, inline form with privileged checkbox

### Key Decisions
- Used `calendar.listEvents` (not `getUpcoming`) for overdue deadlines because `getUpcoming` filters `dueAt >= now` which excludes overdue items
- MatterStatus enum has no `ON_HOLD` value; risk page queries it anyway (will return empty) -- spec was explicit about it
- Search router searches `title` field on matters (not `court`) since schema has `courtId` FK not a text `court` field; title contains client name + matter type which is more useful for search
- Contact model in schema lacks `contactType` field but the contacts router uses it (possibly added via migration); search doesn't include it in select to avoid potential issues
- Documents query in search uses `select` with nested `matter` select (Prisma allows relations inside select but not both select+include)
- Used `Search` icon for both Search nav and Discovery nav (same lucide icon, different pages)
- Audit event for note creation uses eventType `UPDATED` (not `MATTER_UPDATED` which doesn't exist in enum)
